/**
 * Supabase 접속 설정 (Git에 올리지 않음)
 *
 * 1. 이 파일을 config.js 로 복사하세요.
 * 2. Supabase 대시보드에서 프로젝트 URL, anon key 를 확인한 뒤 채우세요.
 * 3. config.js 는 .gitignore 대상이므로 저장소에 포함되지 않습니다.
 */
(function () {
  'use strict';
  window.SUPABASE_URL = 'https://your-project.supabase.co';
  window.SUPABASE_ANON = 'your-anon-key';
})();
